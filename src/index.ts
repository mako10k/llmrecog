/**
 * Public library entrypoint for the initialized package.
 *
 * Product APIs are added only after their grammar, typed result, and fixture
 * contracts are accepted.
 */
export const semanticVersion = "0.1" as const;
